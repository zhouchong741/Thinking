<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.zc741.ke"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/k"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>

        <activity
            android:name=".ItemDrawerleft.AboutActivity"
            android:label="@string/About">
        </activity>
        <activity
            android:name=".ItemDrawerleft.ContributeActivity"
            android:label="@string/submit">
        </activity>
        <activity
            android:name=".ItemDrawerleft.PublishActivity"
            android:label="@string/publish">
        </activity>
        <activity
            android:name=".ItemDrawerleft.VersionActivity"
            android:label="@string/checkupdate">
        </activity>
    </application>

</manifest>
